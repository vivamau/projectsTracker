# Session 2026-05-19 22:35:10

## Task
Add a superadmin "learning phase" box to Settings: builds a RAG embedding index
from meeting notes + all DB tables via a configurable Ollama embedding model,
with an Ollama/model availability health check.

## Decisions
- Ollama can't fine-tune → implemented RAG (embedding index), not training.
- Embedding model name is configurable (default `embeddinggemma`); Ollama base
  URL reused from existing `agent_ollama_url` setting.
- Data scope: meeting notes (.md + title) + all DB tables (internal/noisy
  tables excluded), capped at 500 rows/table.

## Changes (TDD: tests written first)
- Migration `044_rag_embeddings.sql` (rag_embeddings table + index).
- `services/ragService.js` (+ test): getSettings/saveSettings, checkHealth,
  embed, chunkText, gatherCorpus, runLearningPhase, getStatus.
- `routes/ragRoutes.js` (+ test): GET/PUT /rag/settings, GET /rag/health,
  POST /rag/learn — all superadmin-only. Mounted at /api/rag.
- Frontend `api/ragApi.js`, `pages/settings/components/RagLearningCard.jsx`,
  wired into Settings right column (superadmin only).

## Verification
- Full backend suite: 1278 tests / 72 suites pass.
- ragService 85.1% branch / 100% line; ragRoutes 100%.
- Frontend lint: no issues in new files; production build succeeds.

## Follow-up — DONE (user requested after initial build)
- `ragService.cosineSimilarity`, `retrieve(db, q, k)`, `retrieveContext`
  (defensive, never throws) added with tests.
- `agentService.chat` now prepends retrieved context to the user message
  before provider dispatch, wrapped in try/catch so chat never breaks if
  RAG/Ollama is down or the index is empty.
- ragService 88% branch / 100% line; full suite 1287 tests pass.

## Provenance / observability (user requested: "RAG vs MCP" visibility)
- `ragService.retrieveWithContext` → `{ context, sources }`; `retrieveContext`
  now delegates to it.
- `agentService.buildMeta(ragSources, trace)` + a `trace` array threaded
  through all providers (Ollama/Claude/Gemini/OpenAI-compat + wrappers),
  pushing `{type:'sql'}` / `{type:'tool',name}`. `chat()` returns
  `{ ...result, meta:{ ragUsed, ragSources, tools, sqlQueries } }`.
- `ai_token_logs.provenance` TEXT column (auditDatabase schema + ALTER);
  `logTokens` stores meta JSON, `getMessages` returns it.
- `/api/agent/chat` returns `meta`; logs provenance per message.
- Frontend: `Provenance` component in Agent page — RAG/SQL/tool badges +
  expandable source list under each assistant bubble.
- Full suite 1298 tests pass; ragService 88% branch / 100% line; ragRoutes
  100%. Pre-existing flake: userRoutes test under parallel run (unrelated).
  Pre-existing lint: agent/index.jsx `role` unused (not mine).
